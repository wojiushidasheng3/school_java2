spring:
  application:
    name: demo

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://192.168.115.128:5432/postgres
    username: root
    password: 696903
  data:
    redis:
      sentinel:
        master: mymaster
        nodes: 192.168.115.128:27001,192.168.115.128:27002,192.168.115.128:27003
        password: 696903
      password: 696903
      timeout: 5000
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.demo.Entity
  global-config:
    db-config:
      id-type: auto

security:
  jwt:
    secret: demo-jwt-secret-key-for-spring-security-2026
    expiration-millis: 7200000