spring:
  application:
    name: demo

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://192.168.115.128:5432/postgres
    username: root
    password: 696903
  data:
    redis:
      host: 192.168.115.128
      port: 6381
      database: 0
      password: 696903
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.demo.Entity
  global-config:
    db-config:
      id-type: auto

security:
  jwt:
    secret: demo-jwt-secret-key-for-spring-security-2026
    expiration-millis: 7200000